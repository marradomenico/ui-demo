import { HoverPointerDirective } from './hover-pointer.directive';

describe('HoverPointerDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverPointerDirective();
    expect(directive).toBeTruthy();
  });
});
